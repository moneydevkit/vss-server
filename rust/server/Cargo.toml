[package]
name = "server"
version = "0.1.0"
edition = "2021"

[features]
default = []
lambda = ["lambda_runtime", "lambda_http", "serde_json"]

[dependencies]
api = { path = "../api" }
impls = { path = "../impls" }

hyper = { version = "1", default-features = false, features = ["server", "http1"] }
http-body-util = { version = "0.1", default-features = false }
hyper-util = { version = "0.1", default-features = false, features = ["server-graceful"] }
tokio = { version = "1.38.0", default-features = false, features = ["time", "signal", "rt-multi-thread", "macros"] }
prost = { version = "0.11.6", default-features = false, features = ["std"] }
bytes = "1.4.0"
serde = { version = "1.0.203", default-features = false, features = ["derive"] }
toml = { version = "0.8.9", default-features = false, features = ["parse"] }
tower = "0.4"

# AWS SDK dependencies for DynamoDB
aws-config = "1.1.7"
aws-sdk-dynamodb = "1.17.0"

# Lambda dependencies (optional)
lambda_runtime = { version = "0.8.3", optional = true }
lambda_http = { version = "0.8.3", optional = true }
serde_json = { version = "1.0", optional = true }
